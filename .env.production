NODE_ENV=production
PORT=3033

# Database (use service names from docker-compose)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=panda_video
DB_USER=postgres
DB_PASSWORD=your_postgres_password

# Redis (use service names from docker-compose)
REDIS_HOST=redis
REDIS_PORT=6379

# Bull Board
BULL_BOARD_USER=admin
BULL_BOARD_PASSWORD=your_dashboard_password

# Billing
COST_PER_MINUTE=0.50

# Paths
UPLOAD_PATH=/app/uploads
PROCESSED_PATH=/app/processed